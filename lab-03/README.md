# Лабораторная работа №3: Индикатор уровня с динамическим поведением

## Описание проекта

Разработка визуального GUI-компонента на базе `JPanel` — индикатора уровня с тремя цветовыми зонами (нормальная, предупреждение, авария). Компонент способен накапливать данные, отображать текущее состояние и передавать информацию через систему слушателей событий.

## Технические характеристики

### Основной функционал:
- ✅ Вертикальное отображение уровня с пропорциональной высотой
- ✅ Три цветовые зоны: 🟢 норма, 🟡 предупреждение, 🔴 авария
- ✅ Настраиваемые границы зон
- ✅ Накопление исторических данных
- ✅ Система событий для передачи данных
- ✅ Периодическое обновление через слушателей

### Параметры настройки:
| Параметр | Описание | Тип |
|----------|----------|-----|
| `min` | Минимальное значение диапазона | `double` |
| `max` | Максимальное значение диапазона | `double` |
| `criticalLow` | Нижняя граница аварии | `double` |
| `criticalHigh` | Верхняя граница аварии | `double` |
| `warningLow` | Нижняя граница предупреждения | `double` |
| `warningHigh` | Верхняя граница предупреждения | `double` |

## Архитектура системы

### Основные компоненты:

#### 1. **LevelIndicator** (компонент)
Производный класс от `JPanel`, реализующий визуальное отображение индикатора.

**Методы:**
- `setValue(double value)` - установка текущего значения
- `getValue()` - получение текущего значения
- `getAccumulatedData()` - получение накопленных данных
- `clearData()` - очистка накопленных данных
- `addLevelDataListener(LevelDataListener)` - добавление слушателя
- `removeLevelDataListener(LevelDataListener)` - удаление слушателя

#### 2. **LevelIndicatorConfig** (конфигурация)
Класс для хранения параметров настройки индикатора.

#### 3. **LevelDataListener** (слушатель)
Интерфейс для получения периодических данных:
```java
public interface LevelDataListener {
    void onLevelDataUpdate(LevelChangeEvent event);
}
```

#### 4. **LevelDataStorage** (хранилище)
Накопление и хранение исторических данных.

#### 5. **LevelZone** (перечисление)
```java
public enum LevelZone {
    NORMAL,      // Зеленая зона
    WARNING,     // Желтая зона
    CRITICAL     // Красная зона
}
```

## Структура проекта

```
lab-03/
├── src/main/java/pw/ns2030/
│   ├── component/          # GUI компоненты
│   ├── listener/           # Интерфейсы слушателей
│   ├── model/              # Модель данных
│   ├── demo/               # Демонстрация
│   └── Main.java           # Точка входа
├── docs/                   # Документация
└── README.md
```

## Сборка и запуск

### Требования:
- Java 17+
- Gradle 8.0+

### Команды:

```bash
# Компиляция проекта
./gradlew build

# Запуск демонстрации
./gradlew run

# Запуск тестов
./gradlew test

# Создание JAR-файла
./gradlew jar
```

## Демонстрационное приложение

Приложение включает:
1. **Главное окно** с индикатором уровня
2. **Панель управления** с ползунками для изменения значения
3. **Симулятор данных** для автоматической генерации значений
4. **Панель истории** с графиком накопленных данных
5. **Панель настроек** для изменения границ зон

## Примеры использования

### Базовое создание индикатора:
```java
LevelIndicatorConfig config = new LevelIndicatorConfig.Builder()
    .setMinValue(0.0)
    .setMaxValue(100.0)
    .setCriticalLow(0.0, 10.0)
    .setCriticalHigh(90.0, 100.0)
    .setWarningLow(10.0, 30.0)
    .setWarningHigh(70.0, 90.0)
    .build();

LevelIndicator indicator = new LevelIndicator(config);
indicator.setValue(45.0); // Зеленая зона
```

### Подключение слушателя:
```java
indicator.addLevelDataListener(event -> {
    System.out.println("Новое значение: " + event.getValue());
    System.out.println("Зона: " + event.getZone());
    System.out.println("Время: " + event.getTimestamp());
});
```

### Получение накопленных данных:
```java
List<LevelData> history = indicator.getAccumulatedData();
for (LevelData data : history) {
    System.out.printf("Время: %s, Значение: %.2f, Зона: %s%n",
        data.getTimestamp(), data.getValue(), data.getZone());
}
```

## Визуализация

```
┌────────────────┐
│   100.0  ████  │ ← Верхняя граница
│          ████  │
│   90.0   ▓▓▓▓  │ ← Начало красной зоны
│          ▓▓▓▓  │
│   70.0   ▒▒▒▒  │ ← Начало желтой зоны
│          ▒▒▒▒  │
│   50.0   ░░░░  │ ← Зеленая зона (норма)
│          ░░░░  │
│   30.0   ▒▒▒▒  │ ← Конец желтой зоны
│          ▓▓▓▓  │
│   10.0   ▓▓▓▓  │ ← Конец красной зоны
│          ████  │
│    0.0   ████  │ ← Нижняя граница
└────────────────┘
```

## Технологии

- **Java Swing** - графический интерфейс
- **JUnit 5** - тестирование
- **Gradle** - сборка проекта
- **Java Reflection** (опционально для расширенной настройки)

## Паттерны проектирования

1. **Observer** - система слушателей событий
2. **Builder** - построение конфигурации
3. **MVC** - разделение модели, представления и контроллера
4. **Strategy** - различные стратегии отображения зон

## Автор

**Студент:** Шаламов А.Е.  
**Группа:** АВТ-343  
**Преподаватель:** Дыминский И.И.

## Лицензия

Учебный проект НГТУ, 2025